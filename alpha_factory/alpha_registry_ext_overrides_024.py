"""
Compatibility shim for tests that `import alpha_factory.alpha_registry_ext_overrides_024`
for side-effects. If you later need real overrides, place them here.
"""
def _install_overrides() -> None:
    # no-op; keep import succeeding
    return

_install_overrides()
